<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE html>
<html b:css='false' b:defaultwidgetversion='2' b:layoutsVersion='3' expr:dir='data:blog.languageDirection' expr:lang='data:blog.locale' xmlns='http://www.w3.org/1999/xhtml' xmlns:b='http://www.google.com/2005/gml/b' xmlns:data='http://www.google.com/2005/gml/data' xmlns:expr='http://www.google.com/2005/gml/expr'>&lt;!--/**
*
* Created By Oxg Tech
* NihLink v1.0
* Author : Syed Alamdar
* Date	 : 05 November 2025
* URL 	 : https://youtube.com/@oxgtech
* Repository : https://github.com/oxgtech-yt/Oxg-Safelink-Blogger-Template
* License : https://github.com/anasrar/NihLink-Safelink-Template-Blogger/blob/master/LICENSE
*
*/<head>--&gt;<b:include name='body'/>&lt;head&gt;

<meta content='width=device-width, initial-scale=1, user-scalable=yes, maximum-scale=1' name='viewport'/>
<b:if cond='data:blog.pageType == &quot;error_page&quot;'>
  <title>Page Not Found - <data:blog.title/></title>
<b:else/>
  <title><data:view.title.escaped/></title>
</b:if>
<b:include data='blog' name='all-head-content'/>

<link crossorigin='anonymous' href='//stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css' rel='stylesheet'/>
<link href='//use.fontawesome.com/releases/v5.0.10/css/all.css' rel='stylesheet' type='text/css'/>
<script src='//code.jquery.com/jquery-2.1.4.min.js' type='text/javascript'/>
<script src='//cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.3.1/js/bootstrap.bundle.min.js'/>
<script src='//cdnjs.cloudflare.com/ajax/libs/crypto-js/3.1.2/rollups/aes.js'/>

<script type='text/javascript'>
&quot;use strict&quot;;
$(document).ready(function() {
  (function(W,D){
    W.asli_config = {
      url: &#39;&#39;,
      page: &#39;p/lanjut.html&#39;,
      output: &#39;#output&#39;,
      defaultkey: &#39;AnasRAR&#39;,
      fixednavbar: false,
      countdown: true,
      timedown: 10,
      lang: {
        urlempty: &quot;URL can not empty&quot;,
        convertsuccess: &quot;Convert URL success, copy url on box below&quot;,
        validtext: &quot;HTTP, HTTPS, or WWW&quot;,
        gourltext: &quot;coba di klik linknya pak&quot;,
        nourl: &quot;nggapain pak ??, linknya nggak ada&quot;,
        errorconvert: &quot;URL can not to convert&quot;,
        emptypass: &quot;Password can not empty&quot;,
        wrongpass: &quot;Password is incorrect&quot;,
        countdowntext: &quot;Please Wait {{anascountdown}} Second&quot;
      }
    }
    var xv = $(&#39;footer &gt; .d-table &gt; .d-table-row &gt; .d-table-cell &gt; .container &gt; .row &gt; .col-sm-7 &gt; a#naas&#39;);
    if(xv.length){
      W.validurlit = function (ur) {
        return /((([A-Za-z]{3,9}:(?:\/\/)?)(?:[\-;:&amp;=\+\$,\w]+@)?[A-Za-z0-9\.\-]+|(?:www\.|[\-;:&amp;=\+\$,\w]+@)[A-Za-z0-9\.\-]+)((?:\/[\+~%\/\.\w\-_]*)?\??(?:[\-\+=&amp;;%@\.\w_]*)#?(?:[\.\!\/\\\w]*))?)/.test(ur);
      }
      W.sUp = function(el) {
        D.querySelector(el).parentNode.style.height = &#39;0&#39;;
      }
      W._GET = function(name, url) {
        if (!url) url = location.href;
        name = name.replace(/[\[]/,&quot;\\\[&quot;).replace(/[\]]/,&quot;\\\]&quot;);
        var regexS = &quot;[\\?&amp;]&quot;+name+&quot;=([^&amp;#]*)&quot;;
        var regex = new RegExp( regexS );
        var results = regex.exec( url );
        return results == null ? null : results[1];
      }
      xv.attr(&#39;href&#39;, &#39;https://youtube.com/@oxgtech&#39;).text(&#39;Oxg Tech&#39;);
    } else {
      location.href = &#39;https://youtube.com/@oxgtech&#39;;
    }
  }(window,document));
});
</script>

<script type='text/javascript'>
  (function(){
    function initOutputAction(){
      try {
        var sel = (window.config &amp;&amp; window.config.output) ? window.config.output : '#output';
        var out = document.querySelector(sel);
        if(!out || !out.parentNode) return;

        // Hide output until user clicks
        out.style.display = 'none';

        // Define a function to create/show the action button on demand
        window.__showOutputActionButton = function(){
          try {
            if (document.getElementById('output-action-btn')) return; // already created
            var btn = document.createElement('button');
            btn.type = 'button';
            btn.className = 'action-btn';
            btn.id = 'output-action-btn';
            var icon = document.createElement('i');
            icon.className = 'fas fa-arrow-right';
            btn.appendChild(icon);
            btn.appendChild(document.createTextNode(' ' + 'Click Here'));

            // Insert above the output element, centered wrapper
            var wrap = document.createElement('div');
            wrap.style.textAlign = 'center';
            wrap.appendChild(btn);
            out.parentNode.insertBefore(wrap, out);

            btn.addEventListener('click', function(){
              // Disable/hide button and start 10s timer
              btn.disabled = true;
              btn.style.display = 'none';
              // Show output and scroll into view
              out.style.display = '';
              if (typeof out.scrollIntoView === 'function') {
                out.scrollIntoView({ behavior: 'smooth', block: 'start' });
              }
              try {
                if (typeof window.__startOutputTimer === 'function') {
                  window.__startOutputTimer();
                }
              } catch(e) {}
            });
          } catch(e) {}
        };
      } catch(e) {}
    }
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initOutputAction);
    } else {
      initOutputAction();
    }
  })();
</script>

<b:skin version='1.0.0'><![CDATA[


/* LAYOUT WIDGET */
body#layout {width:100%}
body#layout ul {display: none;}
body#layout .layouthide {display:none}


]]></b:skin>

<b:template-skin><![CDATA[
body#layout:before{content:"NihLink v2.6.5";position:absolute;top:33px;right:7px;z-index:1;padding:10px 20px;font-size:18px;font-family:sans-serif;color:#fff;background: #007bff}
body#layout {background-color: #fff; border: 0}
body#layout div.section {background-color: #fff}
body#layout .add_widget a {color: #007bff}
body#layout .row:after {content:'';clear:both;display:block}
body#layout .isHideLayout {display:none;}
body#layout .col-md-3 {float:left;width:30%;}
body#layout .col-md-6 {float:left;width:40%;}
body#layout #page-setting {background: #fffcf2}
]]></b:template-skin>

<style type='text/css'>
	/* FIX */
	.hidden{visibility:hidden;height:0;overflow:hidden;display:none}
	.clear, .clearfix{clear:both}
	.separator a {margin:initial!important;}
	.separator a img{margin: 1em;}
	.separator:nth-child(1) a img{margin:initial;margin-bottom:auto}

	/* BODY */
  * {
    outline: none !important;
  }
	html, body {
	  height: 100%;
	}
	a:hover {
		text-decoration: none !important
	}
  .overflow-hidden {
    overflow: hidden;
  }
  /* Loading Bar (5s) */
  #loadingbar .loading-bar{height:6px;background:#e9ecef;border-radius:3px;overflow:hidden;margin:16px 0}
  #loadingbar .loading-fill{height:100%;background:#007bff;width:0;animation:loadingFill 5s linear forwards}
  @keyframes loadingFill{to{width:100%}}
  /* Modern Loading Bar UI */
  #loadingbar { text-align: center; }
  #loadingbar .loading-container { max-width: 420px; margin: 24px auto; }
  #loadingbar .progress-track { height: 12px; background: #e9ecef; border-radius: 9999px; overflow: hidden; position: relative; box-shadow: inset 0 1px 2px rgba(0,0,0,.06); }
  #loadingbar .progress-fill { height: 100%; width: 0; background: linear-gradient(90deg, #4f46e5 0%, #06b6d4 100%); border-radius: 9999px; transition: width 80ms linear; }
  #loadingbar .progress-text { position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%); font-size: 12px; font-weight: 600; color: #111827; }
  #loadingbar .loading-note { margin-top: 8px; font-size: 12px; color: #6b7280; text-align: center; }
  #loadingbar .scroll-btn { display: inline-flex; align-items: center; gap: 8px; padding: 10px 16px; border: none; border-radius: 9999px; background: linear-gradient(90deg, #4f46e5 0%, #06b6d4 100%); color: #fff; box-shadow: 0 4px 10px rgba(79,70,229,.25); cursor: pointer; font-weight: 600; }
  #loadingbar .scroll-btn:focus { outline: 2px solid rgba(79,70,229,.4); outline-offset: 2px; }
  #loadingbar .scroll-btn i { font-size: 14px; }
  /* Output/Report Loading Bar UI (match loading bar style) */
  #output .output-loading-container, #report .output-loading-container { max-width: 420px; margin: 24px auto; }
  /* Password loading layout: dark background container */
  #report .output-loading-container {
    background: #0a0a0a;
    padding: 12px;
    border-radius: 18px;
    border: 1px solid rgba(255,255,255,0.08);
    box-shadow: 0 12px 30px rgba(0,0,0,0.35);
    /* Avoid clipping with floated images or ancestor overflow */
    clear: both;
    position: relative;
    z-index: 2;
    overflow: visible;
  }
  /* Ensure the report area itself doesn’t clip children */
  #report { overflow: visible; }
  #output .output-progress-track, #report .output-progress-track { height: 12px; background: #e9ecef; border-radius: 9999px; overflow: hidden; position: relative; box-shadow: inset 0 1px 2px rgba(0,0,0,.06); }
  #output .output-progress-fill, #report .output-progress-fill { height: 100%; width: 0; background: linear-gradient(90deg, #4f46e5 0%, #06b6d4 100%); border-radius: 9999px; transition: width 80ms linear; }
  #output .output-progress-text, #report .output-progress-text { position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%); font-size: 12px; font-weight: 600; color: #111827; }
  #output .output-loading-note, #report .output-loading-note { margin-top: 8px; font-size: 12px; color: #6b7280; text-align: center; }
  /* Spacing below loading bar and button */
  #output .output-loading-container { margin-bottom: 48px; }
  #report .output-loading-container { margin-bottom: 48px; }
  #output .go-btn, #report .go-btn { margin-bottom: 36px; }
  /* Action button (same design as loading bar button) */
  .action-btn { display: inline-flex; align-items: center; gap: 8px; padding: 10px 16px; border: none; border-radius: 9999px; background: linear-gradient(90deg, #4f46e5 0%, #06b6d4 100%); color: #fff; box-shadow: 0 4px 10px rgba(79,70,229,.25); cursor: pointer; font-weight: 600; }
  .action-btn:focus { outline: 2px solid rgba(79,70,229,.4); outline-offset: 2px; }
  .action-btn i { font-size: 14px; }
  /* Final Go button (unique design) */
  .go-btn { display: inline-flex; align-items: center; gap: 10px; padding: 12px 20px; border-radius: 9999px; text-decoration: none; border: none; background: linear-gradient(90deg, #22c55e 0%, #06b6d4 50%, #9333ea 100%); color: #ffffff; font-weight: 700; letter-spacing: .01em; box-shadow: 0 8px 20px rgba(34,197,94,.25), 0 2px 8px rgba(147,51,234,.25); }
  .go-btn:hover { filter: brightness(1.05); transform: translateY(-1px); transition: transform 150ms ease, filter 150ms ease; }
  .go-btn:focus { outline: 2px solid rgba(147,51,234,.35); outline-offset: 2px; }
  .go-btn i { font-size: 16px; }
	.trans {
	  -webkit-transition: all .5s ease-in-out;
	  -moz-transition: all .5s ease-in-out;
	  -ms-transition: all .5s ease-in-out;
	  -o-transition: all .5s ease-in-out;
	  transition: all .5s ease-in-out;
	}
	.hidding-smooth {
	  position: relative;
	  height: 6vh;
	  overflow: hidden;
	}
	.hidding-smooth:after {
	  -webkit-transition: ease 0.8s;
	  -moz-transition: ease 0.8s;
	  -ms-transition: ease 0.8s;
	  -o-transition: ease 0.8s;
	  transition: ease 0.8s;
	  position: absolute;
	  bottom: 0;
	  left: 0;
	  right: 0;
	  content: &#39;&#39;;
	  background: linear-gradient(180deg, transparent, white 70%);
	  width: 100%;
	  height: 5vh;
	}
	.hidding-smooth:hover:after {
	  height: 1px;
	}
	#fullpage {
	  min-height: 100%;
	  margin: 0 0 -120px 0;
	  padding: 0 0 120px 0;
	}
	footer.footer {
	  height: 120px;
	  background-color: #ffffff;
	  border-top: 1px solid #dee2e6;
	}
	.sticky-top {
	  top: calc(60px + 1rem)
    }
	@media (max-width: 767px) {
    .order-custom {
        -webkit-box-ordinal-group: 2;
        -ms-flex-order: 1;
        order: 1
    }
  }
</style>

&lt;!--</head>--&gt;&lt;/head&gt;
&lt;body itemscope=&#39;itemscope&#39; itemtype=&#39;http://schema.org/WebPage&#39;&gt;

<div id='fullpage'>
  <div class='container-semifluid'>
    <div class='holder'>
      <nav class='navbar navbar-expand border-bottom bg-white py-2' data-type='fixednav'>
        <div class='container'>
          <a class='navbar-brand' expr:href='data:blog.homepageUrl'><data:blog.title/></a>
          <button aria-controls='navbarSupportedContent' aria-expanded='false' aria-label='Toggle navigation' class='navbar-toggler' data-target='#navbarSupportedContent' data-toggle='collapse' type='button'>
            <span class='navbar-toggler-icon'/>
          </button>

          <div class='collapse navbar-collapse justify-content-end' id='navbarSupportedContent'>
            <ul class='navbar-nav ml-auto'>
              <li class='nav-item dropdown'>
                <a aria-expanded='false' aria-haspopup='true' class='nav-link dropdown-toggle px-0' data-toggle='dropdown' href='#' id='navbarDropdown' role='button'>
                  Menu
                </a>
                <ul aria-labelledby='navbarDropdown' class='dropdown-menu dropdown-menu-right' itemprop='mainEntity' itemscope='itemscope' itemtype='http://schema.org/SiteNavigationElement'>
                  <li class='dropdown-item'><a href='https://github.com/oxgtech-yt/Oxg-Safelink-Blogger-Template' itemprop='url' title='Documentation'><span itemprop='name'>Documentation</span></a></li>
                  <div class='dropdown-divider'/>
                  <li class='dropdown-item'><a href='https://youtube.com/@oxgtech' itemprop='url' title='Oxg Tech'><span itemprop='name'>Oxg Tech</span></a></li>
                </ul>
              </li>
            </ul>
          </div>
        </div>
      </nav>
    </div>
    <div class='container py-3'>
      <div id='isihalaman'>


        <b:section class='page-setting' cond='!data:view.isPreview' id='page-setting' maxwidgets='1'>
          <b:widget id='HTML696' locked='false' title='setting' type='HTML' version='2' visible='true'>
            <b:widget-settings>
              <b:widget-setting name='content'><![CDATA[url: "",
page: "p/lanjut.html",
output: "#output",
fixednavbar: true,
countdown: true,
timedown: 10,
lang: {
 urlempty: "URL can not be empty",
 convertsuccess: "Convert URL success, copy url on box below",
 validtext: "HTTP, HTTPS, or WWW",
 gourltext: "Click here to go",
 nourl: "No URl here",
 errorconvert: "URL can not to convert",
 emptypass: "Password can not be empty",
 wrongpass: "Password is incorrect",
 countdowntext: "Please Wait {{anascountdown}} Second"
}]]></b:widget-setting>
            </b:widget-settings>
            <b:includable id='main'>
<script type='text/javascript'>
  &quot;use strict&quot;;
  $(document).ready(function() {
    window.new_config = {
      <data:content/>
    }
    window.config = $.extend(true, asli_config, new_config);
  });
</script>
            </b:includable>
          </b:widget>
        </b:section>


        <div class='row'>

          <div class='col-12 col-md-3 mb-3'>
            <b:section class='ads-left sticky-top' cond='!data:view.isPreview' id='ads-left' maxwidgets='1'>
              <b:widget id='HTML602' locked='false' title='left-sidebar-ads' type='HTML' version='2' visible='true'>
                <b:widget-settings>
                  <b:widget-setting name='content'><![CDATA[<img src="//dummyimage.com/700x700/ccc/777.png&text=+++ADS+++" alt="text=ads" title="ads" class="d-block w-100 mb-3" /><img src="//dummyimage.com/700x700/ccc/777.png&text=+++ADS+++" alt="text=ads" title="ads" class="d-block w-100 mb-3" />]]></b:widget-setting>
                </b:widget-settings>
                <b:includable id='main'>
      						<b:if cond='data:view.isSingleItem'>
      							<data:content/>
      						</b:if>
      					</b:includable>
              </b:widget>
            </b:section>
          </div>


          <div class='col-12 col-md-6 pb-3'>


            <b:if cond='data:blog.pageType == &quot;error_page&quot;'>
              <div class='text-center' style='font-size: 7vw'>Oppss...</div>
              <div class='text-center' style='font-size: 3vw'>Page not found</div>
            </b:if>


            <b:if cond='data:view.isSingleItem'>
              <script type='text/javascript'>
                &quot;use strict&quot;;

if (!String.prototype.startsWith) {
    Object.defineProperty(String.prototype, &#39;startsWith&#39;, {
        value: function(search, pos) {
            pos = !pos || pos &lt; 0 ? 0 : +pos;
            return this.substring(pos, pos + search.length) === search;
        }
    });
}

                $(document).ready(function(){
                  $(document).on(&#39;submit&#39;, &#39;#locked&#39;, function(e) {
                    e.preventDefault();
                    var $this = $(this),
                    parent = $this.parent(),
                    report = parent.find(&#39;#report&#39;),
                    passin = $(&#39;#passin&#39;);

					var data;
                    try {
          			  data = JSON.parse(CryptoJS.AES.decrypt(decodeURIComponent(_GET(&#39;u&#39;)), passin[0].value).toString(CryptoJS.enc.Utf8));
      				} catch (e) {
                      data = false;
                  	}

                    if (passin[0].value.length) {
                      if (typeof(data) === &quot;object&quot;) {
                        if(!data.countdown){
                          report[0].innerHTML = &#39;&lt;div class=&quot;text-center&quot;&gt;&lt;a class=&quot;go-btn&quot; href=&quot;&#39;+(data.url.toLowerCase().startsWith(&#39;www.&#39;) ? &#39;//&#39; : &#39;&#39;)+&#39;&#39;+data.url+&#39;&quot;&gt;&lt;i class=&quot;fas fa-external-link-alt&quot;&gt;&lt;/i&gt; &#39;+config.lang.gourltext+&#39;&lt;/a&gt;&lt;/div&gt;&#39;;
                          try { report[0].parentNode.style.height = &#39;auto&#39;; report[0].parentNode.style.overflow = &#39;visible&#39;; } catch(e) {}
                          $this.find(&#39;button, input&#39;).attr(&#39;disabled&#39;, &#39;disabled&#39;);
                          $this.slideUp(&#39;slow&#39;);
						} else {
                          $this.find(&#39;button, input&#39;).attr(&#39;disabled&#39;, &#39;disabled&#39;);
                          $this.slideUp(&#39;slow&#39;);
                          // Build modern loading bar UI and smooth countdown
                          var duration = (config.timedown || 10) * 1000;
                          report[0].innerHTML = '';
                          var container = document.createElement('div');
                          container.className = 'output-loading-container';
                          var track = document.createElement('div');
                          track.className = 'output-progress-track';
                          var fill = document.createElement('div');
                          fill.className = 'output-progress-fill';
                          track.appendChild(fill);
                          var text = document.createElement('div');
                          text.className = 'output-progress-text';
                          text.textContent = (config.timedown || 10) + 's';
                          track.appendChild(text);
                          container.appendChild(track);
                          var note = document.createElement('div');
                          note.className = 'output-loading-note';
                          note.textContent = 'Please Wait...';
                          container.appendChild(note);
                          report[0].appendChild(container);
                          try { report[0].parentNode.style.height = &#39;auto&#39;; report[0].parentNode.style.overflow = &#39;visible&#39;; } catch(e) {}

                          var start = Date.now();
                          var MuANasRAR = setInterval(function(){
                            var elapsed = Date.now() - start;
                            var pct = Math.min(elapsed / duration * 100, 100);
                            fill.style.width = pct + '%';
                            var remaining = Math.ceil((duration - elapsed)/1000);
                            text.textContent = (remaining > 0 ? remaining : 0) + 's';
                            if (elapsed >= duration) {
                              clearInterval(MuANasRAR);
                              var wrap = document.createElement('div');
                              wrap.className = 'text-center';
                              var link = document.createElement('a');
                              link.className = 'go-btn';
                              link.href = (data.url.toLowerCase().startsWith('www.') ? '//' : '') + data.url;
                              var ic = document.createElement('i'); ic.className = 'fas fa-external-link-alt';
                              link.appendChild(ic);
                              link.appendChild(document.createTextNode(' ' + config.lang.gourltext));
                              wrap.appendChild(link);
                              report[0].innerHTML = '';
                              report[0].appendChild(wrap);
                            }
                          }, 80);
						}
                      } else {
                        report[0].innerHTML = &#39;&lt;div class=&quot;alert alert-danger text-center&quot;&gt;&#39;+config.lang.wrongpass+&#39;&lt;/div&gt;&#39;;
                      }
                    } else {
                      report[0].innerHTML = &#39;&lt;div class=&quot;alert alert-danger text-center&quot;&gt;&#39;+config.lang.emptypass+&#39;&lt;/div&gt;&#39;;
                    }

                    setTimeout(function(){
                      report[0].parentNode.style.height = report[0].offsetHeight+&#39;px&#39;;
                    }, 0);
                  });


                  if (_GET(&#39;u&#39;) &amp;&amp; $(config.output).length) {

					var data;
                    try {
          			  data = JSON.parse(CryptoJS.AES.decrypt(decodeURIComponent(_GET(&#39;u&#39;)), config.defaultkey).toString(CryptoJS.enc.Utf8));
      				} catch (e) {
                      data = false;
                  	}

                    // Store data globally and define click-started timer
                    window.__outputData = data;
                    window.__startOutputTimer = function(){
                      try {
                        var outEl = $(config.output)[0];
                        var d = window.__outputData;
                        if (!outEl || typeof d !== 'object') return;

                        // Direct link if countdown disabled
                        if(!d.countdown){
                          var wrap = document.createElement('div');
                          wrap.className = 'text-center';
                          var link = document.createElement('a');
                          link.className = 'go-btn';
                          link.href = (d.url.toLowerCase().startsWith('www.') ? '//' : '') + d.url;
                          var ic = document.createElement('i'); ic.className = 'fas fa-external-link-alt';
                          link.appendChild(ic);
                          link.appendChild(document.createTextNode(' ' + config.lang.gourltext));
                          wrap.appendChild(link);
                          outEl.innerHTML = '';
                          outEl.appendChild(wrap);
                          return;
                        }

                        // Build output loading bar UI (10s)
                        outEl.innerHTML = '';
                        var container = document.createElement('div');
                        container.className = 'output-loading-container';
                        var track = document.createElement('div');
                        track.className = 'output-progress-track';
                        var fill = document.createElement('div');
                        fill.className = 'output-progress-fill';
                        track.appendChild(fill);
                        var text = document.createElement('div');
                        text.className = 'output-progress-text';
                        text.textContent = '10s';
                        track.appendChild(text);
                        container.appendChild(track);
                        var note = document.createElement('div');
                        note.className = 'output-loading-note';
                        note.textContent = 'Please Wait';
                        container.appendChild(note);
                        outEl.appendChild(container);

                        var duration = 10000;
                        var start = Date.now();
                        var timer = setInterval(function(){
                          var elapsed = Date.now() - start;
                          var pct = Math.min(elapsed / duration * 100, 100);
                          fill.style.width = pct + '%';
                          var remaining = Math.ceil((duration - elapsed)/1000);
                          text.textContent = (remaining > 0 ? remaining : 0) + 's';
                          if (elapsed >= duration) {
                            clearInterval(timer);
                            // Replace with final link
                            var wrap = document.createElement('div');
                            wrap.className = 'text-center';
                            var link = document.createElement('a');
                            link.className = 'go-btn';
                            link.href = (d.url.toLowerCase().startsWith('www.') ? '//' : '') + d.url;
                            var ic = document.createElement('i'); ic.className = 'fas fa-external-link-alt';
                            link.appendChild(ic);
                            link.appendChild(document.createTextNode(' ' + config.lang.gourltext));
                            wrap.appendChild(link);
                            outEl.innerHTML = '';
                            outEl.appendChild(wrap);
                          }
                        }, 80);
                      } catch(e) {}
                    };

                    // Do not auto-start here; output remains until user clicks the button
                    if (typeof(data) !== &quot;object&quot;) {
                      $(config.output)[0].innerHTML = &#39;&lt;form class=&quot;form-group&quot; id=&quot;locked&quot;&gt;&lt;div class=&quot;input-group&quot;&gt;&lt;div class=&quot;input-group-prepend&quot;&gt;&lt;span class=&quot;input-group-text&quot;&gt;&lt;i class=&quot;fas fa-lock fa-fw&quot;&gt;&lt;/i&gt;&lt;/span&gt;&lt;/div&gt;&lt;input class=&quot;form-control&quot; name=&quot;pass&quot; type=&quot;password&quot; id=&quot;passin&quot; onclick=&quot;sUp(\&#39;#report\&#39;)&quot; onkeypress=&quot;sUp(\&#39;#report\&#39;)&quot; aria-label=&quot;Password&quot; /&gt;&lt;div class=&quot;input-group-append&quot;&gt;&lt;button class=&quot;btn btn-primary&quot; type=&quot;submit&quot;&gt;Unlock&lt;/button&gt;&lt;/div&gt;&lt;/div&gt;&lt;/form&gt;&lt;section class=&quot;trans&quot; style=&quot;overflow: visible;height: auto&quot;&gt;&lt;div id=&quot;report&quot;&gt;&lt;/div&gt;&lt;/section&gt;&#39;;
                    }
                  } else {
                    if($(config.output).length){
                      $(config.output)[0].innerHTML = &#39;&lt;div class=&quot;text-center&quot;&gt;&#39;+config.lang.nourl+&#39;&lt;/div&gt;&#39;;
                    } else {
                      console.log(&#39;Not found output element, are you sure you this is not safelink page ??&#39;);
                    }
                  }
                });
              </script>
            </b:if>

            <b:if cond='data:view.isMultipleItems'>

              <section>
                <form autocomplete='off' class='form-group' id='safelink'>
                  <div class='input-group mb-0'>
                    <div class='input-group-prepend'>
                      <span class='input-group-text'><i class='fas fa-link fa-fw'/></span>
                    </div>
                    <input class='form-control' id='urlinput' onclick='sUp(&apos;#result&apos;)' onkeypress='sUp(&apos;#result&apos;)' placeholder='HTTP, HTTPS, or WWW' type='text'/>
                    <div class='input-group-append'>
                      <button class='btn btn-primary' type='submit'>Convert</button>
                    </div>
                  </div>
                </form>
              </section>

              <section>
                <div class='row mb-2'>
                  <div class='col-3'>
                    <button class='btn btn-dark btn-block' id='passbtn' onclick='sUp(&apos;#result&apos;)'>Password</button>
                  </div>
                  <div class='col-9'>
                    <div class='input-group mb-0'>
                      <div class='input-group-prepend'>
                        <span class='input-group-text'><i class='fas fa-lock fa-fw'/></span>
                      </div>
                      <input class='form-control' disabled='disabled' id='passinput' onclick='sUp(&apos;#result&apos;)' onkeypress='sUp(&apos;#result&apos;)' type='text'/>
                    </div>
                  </div>
                </div>
              </section>

              <section class='mb-3' style='display:none'>
                <div class='d-inline-block custom-control custom-switch mr-3'>
                  <input class='custom-control-input' id='randPost' onclick='sUp(&apos;#result&apos;)' type='checkbox'/>
                  <label class='custom-control-label' for='randPost' onclick='sUp(&apos;#result&apos;)'>Random Post</label>
                </div>
                <div class='d-inline-block custom-control custom-switch'>
                  <input class='custom-control-input' id='countDown' onclick='sUp(&apos;#result&apos;)' type='checkbox'/>
                  <label class='custom-control-label' for='countDown' onclick='sUp(&apos;#result&apos;)'>Countdown</label>
                </div>
              </section>

              <section class='trans' style='overflow: hidden;height: 0'>
                <div id='result'/>
              </section>

              <section class='layouthide'>
                <div class='row text-center mb-3' style='margin-top:20%'>
                  <div class='col-12 col-sm-4'>
                    <div style='height: 60px; overflow: hidden'>
                      <i class='fas fa-eye fa-fw fa-3x'/>
                    </div>
                    <h5>Encrypt</h5>
                    <p>Make your url unreadable and no one can bypass</p>
                  </div>
                  <div class='col-12 col-sm-4'>
                    <div style='height: 60px; overflow: hidden'>
                      <i class='fas fa-lock fa-fw fa-3x'/>
                    </div>
                    <h5>Password</h5>
                    <p>Set a password to protect your links from unauthorized access</p>
                  </div>
                  <div class='col-12 col-sm-4'>
                    <div style='height: 60px; overflow: hidden'>
                      <i class='fas fa-cogs fa-fw fa-3x'/>
                    </div>
                    <h5>Setting</h5>
                      <p>Easy to setting without need to know programming language</p>
                    </div>
                  </div>
              </section>

              <script type='text/javascript'>
              &quot;use strict&quot;;
                $(document).ready(function() {

                  config.countdown ? $(&#39;#countDown&#39;).prop(&#39;checked&#39;, true) : $(&#39;#countDown&#39;).prop(&#39;checked&#39;, false) ;
                  $(&#39;#randPost&#39;).prop(&#39;checked&#39;, true);

                  $(&#39;#passbtn&#39;).on(&#39;click&#39;, function(e) {
                    var $this = $(this);
                      if($this.hasClass(&#39;btn-dark&#39;)){
                        $this.removeClass(&#39;btn-dark&#39;).addClass(&#39;btn-primary&#39;);
                        $(&#39;#passinput&#39;).removeAttr(&#39;disabled&#39;);
                      } else {
                        $this.removeClass(&#39;btn-success&#39;).addClass(&#39;btn-dark&#39;);
                        $(&#39;#passinput&#39;).attr(&#39;disabled&#39;, &#39;disabled&#39;)[0].value = &#39;&#39;;
                      }
                    e.preventDefault();
                  });

                  $(&#39;#safelink&#39;).on(&#39;submit&#39;, function(e) {
                    e.preventDefault();
                    var $this = $(this),
                    passinput = $(&#39;#passinput&#39;)[0],
                    keyit = passinput.value.length ? passinput.value : config.defaultkey,
                    blog = config.url.length ? config.url : window.location.protocol + &quot;//&quot; + window.location.hostname,
                    url = $this.find(&#39;#urlinput&#39;)[0],
                    randPost = $(&#39;#randPost&#39;)[0],
                    result = $(&#39;#result&#39;)[0],
                    data = {};
                    data.url = url.value,
                    data.countdown = $(&#39;#countDown&#39;)[0].checked;

                    if (url.value.length) {
                      if (randPost.checked) {
                        if (validurlit(url.value)) {
                          $.ajax({
                            url: &#39;/feeds/posts/summary?alt=json&amp;max-results=99&#39;,
                            type: &#39;GET&#39;,
                            dataType: &#39;json&#39;,
                            cache: true,
                            beforeSend: function() {
                              result.innerHTML = &#39;&lt;div class=&quot;text-center&quot;&gt;&lt;span class=&quot;spinner-border spinner-border-sm text-primary&quot; role=&quot;status&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt; Fetch Post&lt;/div&gt;&#39; ;
                            },
                            success: function(a){
                              var post = a.feed.entry,
                                  randNum = Math.floor(Math.random() * post.length),
                                  linknya = &quot;&quot;;
                              for(var i = 0; i &lt; post[randNum].link.length; i++){
                                if(post[randNum].link[i].rel == &#39;alternate&#39;) {
                                    linknya = post[randNum].link[i].href;
                                  break;
                                }
                              }
                              result.innerHTML = &#39;&lt;div class=&quot;alert alert-success text-center&quot;&gt;&#39;+config.lang.convertsuccess+&#39;&lt;/div&gt;&lt;div class=&quot;input-group mb-0&quot;&gt;&lt;div class=&quot;input-group-prepend&quot;&gt;&lt;span class=&quot;input-group-text&quot;&gt;&lt;i class=&quot;fas fa-copy fa-fw&quot;&gt;&lt;/i&gt;&lt;/span&gt;&lt;/div&gt;&lt;input class=&quot;form-control&quot; onfocus=&quot;this.select()&quot; onmouseup=&quot;return false&quot; style=&quot;box-shadow: 0 0 0 0 transparent&quot; value=&quot;&#39;+linknya+&#39;?u=&#39;+encodeURIComponent(CryptoJS.AES.encrypt(JSON.stringify(data), keyit))+&#39;&quot;/&gt;&lt;/div&gt;&#39;;
                              setTimeout(function(){
                                result.parentNode.style.height = result.offsetHeight+&#39;px&#39;;
                              }, 0);
                            }
                          });
                          // result.innerHTML = &#39;&lt;div class=&quot;text-center&quot;&gt;&lt;span class=&quot;spinner-border spinner-border-sm text-primary&quot; role=&quot;status&quot; aria-hidden=&quot;true&quot;&gt;&lt;/span&gt; Fetch Post&lt;/div&gt;&#39; ;
                        } else {
                          result.innerHTML = &#39;&lt;div class=&quot;alert alert-warning text-center&quot;&gt;&#39;+config.lang.validtext+&#39;&lt;/div&gt;&#39; ;
                        }
                      } else {
                        result.innerHTML = validurlit(url.value) ? &#39;&lt;div class=&quot;alert alert-success text-center&quot;&gt;&#39;+config.lang.convertsuccess+&#39;&lt;/div&gt;&lt;div class=&quot;input-group mb-0&quot;&gt;&lt;div class=&quot;input-group-prepend&quot;&gt;&lt;span class=&quot;input-group-text&quot;&gt;&lt;i class=&quot;fas fa-copy fa-fw&quot;&gt;&lt;/i&gt;&lt;/span&gt;&lt;/div&gt;&lt;input class=&quot;form-control&quot; onfocus=&quot;this.select()&quot; onmouseup=&quot;return false&quot; style=&quot;box-shadow: 0 0 0 0 transparent&quot; value=&quot;&#39;+blog+&#39;/&#39;+config.page+&#39;?u=&#39;+encodeURIComponent(CryptoJS.AES.encrypt(JSON.stringify(data), keyit))+&#39;&quot;/&gt;&lt;/div&gt;&#39; : &#39;&lt;div class=&quot;alert alert-warning text-center&quot;&gt;&#39;+config.lang.validtext+&#39;&lt;/div&gt;&#39; ;
                      }
                    } else {
                      result.innerHTML = &#39;&lt;div class=&quot;alert alert-danger text-center&quot;&gt;&#39;+config.lang.urlempty+&#39;&lt;/div&gt;&#39;;
                    }


                    setTimeout(function(){
                      result.parentNode.style.height = result.offsetHeight+&#39;px&#39;;
                    }, 0);
                  });

                });
              </script>
            </b:if>

            <b:section id='page-is' maxwidgets='1' showaddelement='no'>
              <b:widget cond='data:view.isSingleItem' id='Blog1' locked='true' title='Blog Posts' type='Blog' version='2' visible='true'>
                <b:widget-settings>
                  <b:widget-setting name='showDateHeader'>false</b:widget-setting>
                  <b:widget-setting name='commentLabel'>Comment</b:widget-setting>
                  <b:widget-setting name='style.textcolor'>#ffffff</b:widget-setting>
                  <b:widget-setting name='showShareButtons'>true</b:widget-setting>
                  <b:widget-setting name='showCommentLink'>true</b:widget-setting>
                  <b:widget-setting name='style.urlcolor'>#ffffff</b:widget-setting>
                  <b:widget-setting name='postLocationLabel'>Location</b:widget-setting>
                  <b:widget-setting name='showAuthor'>true</b:widget-setting>
                  <b:widget-setting name='style.linkcolor'>#ffffff</b:widget-setting>
                  <b:widget-setting name='style.unittype'>TextAndImage</b:widget-setting>
                  <b:widget-setting name='style.bgcolor'>#ffffff</b:widget-setting>
                  <b:widget-setting name='timestampLabel'>-</b:widget-setting>
                  <b:widget-setting name='reactionsLabel'/>
                  <b:widget-setting name='showAuthorProfile'>true</b:widget-setting>
                  <b:widget-setting name='style.layout'>1x1</b:widget-setting>
                  <b:widget-setting name='showLabels'>true</b:widget-setting>
                  <b:widget-setting name='showLocation'>false</b:widget-setting>
                  <b:widget-setting name='postLabelsLabel'>Labels</b:widget-setting>
                  <b:widget-setting name='showTimestamp'>true</b:widget-setting>
                  <b:widget-setting name='postsPerAd'>1</b:widget-setting>
                  <b:widget-setting name='showBacklinks'>false</b:widget-setting>
                  <b:widget-setting name='style.bordercolor'>#ffffff</b:widget-setting>
                  <b:widget-setting name='showInlineAds'>false</b:widget-setting>
                  <b:widget-setting name='showReactions'>false</b:widget-setting>
                </b:widget-settings>
                <b:includable id='main' var='this'>
                  <b:comment>var=&#39;this&#39; digunakan untuk mengirim setiap data allBylineItems agar bisa terbaca</b:comment>

             	    <b:loop values='data:posts' var='post'>
            		    <b:include name='article.loop'/>
            	    </b:loop>

                </b:includable>
                <b:includable id='aboutPostAuthor'/>
                <b:includable id='addComments'/>
                <b:includable id='article.loop'>
                  <b:if cond='data:view.isMultipleItems'>

                    <article expr:class='&quot;content index post-&quot; + data:post.id + &quot; blog&quot;' id='content-card' itemscope='itemscope' itemtype='https://schema.org/BlogPosting'>
            	         <b:include name='article.loop.index'/>
                     </article>

                  <b:else/>

                    <main aria-label='Main content' class='content post' itemscope='itemscope' itemtype='https://schema.org/Blog'>
            	         <article expr:class='&quot;post-&quot; + data:post.id + &quot; blog&quot;' itemprop='blogPost' itemscope='itemscope' itemtype='https://schema.org/BlogPosting'>
            		           <b:include name='article.loop.post'/>
            	         </article>
                    </main>
                  </b:if>
                </b:includable>
                <b:includable id='article.loop.index'>

            <div class='thumbnail'>
            <img class='trans'>
            <b:attr expr:value='data:post.title ? data:post.title : data:messages.image' name='alt'/>
            <b:attr expr:value='data:post.title ? data:post.title : data:messages.image' name='title'/>
            <b:if cond='data:post.featuredImage'>
            <b:attr expr:value='resizeImage(data:post.featuredImage, 240)' name='src'/>
            <b:else/>
            <b:attr name='src' value='https://placehold.it/750x440/4a138c/fff?text=NO+THUMBNAIL'/>
            </b:if>
            </img>
            </div>
            <a class='btn' expr:href='data:post.link ? data:post.link : data:post.url' expr:title='data:post.title ? data:post.title : data:messages.noTitle'/>
            <h3>
            	<a expr:href='data:post.link ? data:post.link : data:post.url' expr:title='data:post.title ? data:post.title : data:messages.noTitle'>
            		<b:eval expr='data:post.title ? data:post.title snippet { length: 5, links: false, linebreaks: false, ellipsis: true } : data:messages.noTitle'/>
            	</a>
            </h3>
            <div class='deskripsi'><b:eval expr='data:post.snippets.long snippet { length: 100, links: false, linebreaks: false, ellipsis: true }'/></div>

            </b:includable>
                <b:includable id='article.loop.post'>

            <div class='post-body' itemprop='articleBody'>
              <h3 class='text-center'><data:post.title/></h3>
            	<data:post.body/>
            </div>

            </b:includable>
                <b:includable id='article.pagination'>
            <b:if cond='not data:view.isSingleItem'>
            <b:if cond='data:olderPageUrl'>
            <div class='col-sm-12 col-xs-12'>
            <div id='linkloadmore'>
            	<a expr:href='data:olderPageUrl' expr:id='data:widget.instanceId + &quot;_pagination-old&quot;' expr:title='data:messages.olderPosts' itemprop='url'>
            		<span itemprop='name'>Next Page</span>
            	</a>
            </div>
            </div>
            </b:if>
            </b:if>
            </b:includable>
                <b:includable id='commentAuthorAvatar'/>
                <b:includable id='commentDeleteIcon'/>
                <b:includable id='commentForm'/>
                <b:includable id='commentFormIframeSrc'/>
                <b:includable id='commentItem'/>
                <b:includable id='commentList'/>
                <b:includable id='commentPicker'/>
                <b:includable id='comments'/>
                <b:includable id='commentsTitle'/>
                <b:includable id='feedLinks'/>
                <b:includable id='feedLinksBody'/>
                <b:includable id='homePageLink'/>
                <b:includable id='iframeComments'/>
                <b:includable id='inlineAd'/>
                <b:includable id='nextPageLink'/>
                <b:includable id='post'/>
                <b:includable id='postBody'/>
                <b:includable id='postBodySnippet'/>
                <b:includable id='postCommentsAndAd'/>
                <b:includable id='postCommentsLink'/>
                <b:includable id='postFooter'/>
                <b:includable id='postFooterAuthorProfile'/>
                <b:includable id='postHeader'/>
                <b:includable id='postJumpLink'/>
                <b:includable id='postMeta'/>
                <b:includable id='postPagination'/>
                <b:includable id='postTitle'/>
                <b:includable id='previousPageLink'/>
                <b:includable id='status-message'/>
                <b:includable id='threadedCommentForm'/>
                <b:includable id='threadedCommentJs'/>
                <b:includable id='threadedComments'/>
                <b:includable id='tooltipCss'>
  <!-- LINT.IfChange -->
  <style>
    .post-body a.b-tooltip-container {
      position: relative;
      display: inline-block;
    }

    .post-body a.b-tooltip-container .b-tooltip {
      display: block !important;
      position: absolute;
      top: 100%;
      left: 50%;
      transform: translate(-20%, 1px);
      visibility: hidden;
      opacity: 0;
      z-index: 1;
      transition: opacity 0.2s ease-in-out;
    }

    .post-body a.b-tooltip-container .b-tooltip iframe {
      width: 200px;
      height: 198px;
      max-width: none;
      border: none;
      border-radius: 20px;
      box-shadow: 1px 1px 3px 1px rgba(0, 0, 0, 0.2);
    }

    @media (hover: hover) {
      .post-body a.b-tooltip-container:hover .b-tooltip {
        visibility: visible;
        opacity: 1;
      }
    }
  </style>
  <!-- LINT.ThenChange(//depot/google3/java/com/google/blogger/b2/layouts/widgets/v2-style.css) -->
</b:includable>
              </b:widget>
            </b:section>

            <b:section cond='false' id='reset'/>

          </div>


          <div class='col-12 col-md-3 order-last mb-3'>
            <b:section class='ads-right sticky-top' cond='!data:view.isPreview' id='ads-right' maxwidgets='1'>
              <b:widget id='HTML704' locked='false' title='right-sidebar-ads' type='HTML' version='2' visible='true'>
                <b:widget-settings>
                  <b:widget-setting name='content'><![CDATA[<img src="//dummyimage.com/700x700/ccc/777.png&text=+++ADS+++" alt="text=ads" title="ads" class="d-block w-100 mb-3" /><img src="//dummyimage.com/700x700/ccc/777.png&text=+++ADS+++" alt="text=ads" title="ads" class="d-block w-100 mb-3" />]]></b:widget-setting>
                </b:widget-settings>
                <b:includable id='main'>
          				<b:if cond='data:view.isSingleItem'>
          					<data:content/>
          				</b:if>
          			</b:includable>
              </b:widget>
            </b:section>
          </div>

        </div>

      </div>
    </div>
  </div>
</div>

<footer class='footer'>
  <div class='d-table w-100' style='height:100%'>
    <div class='d-table-row'>
      <div class='d-table-cell align-middle'>
        <div class='container'>
          <div class='row'>
            <div class='col-sm-5'>
              &#169; 2025 <a expr:href='data:blog.homepageUrl' expr:title='data:blog.title'><span expr:title='data:blog.title'><data:blog.title/></span></a> All Right Reserved
            </div>
            <div class='col-sm-7 text-right'>
              Created By <a href='//anasrar.github.io' id='naas' title='Anas RAR'>Anas RAR</a>
              <!-- Please Dont Remove Or Change My Link  -->
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</footer>

<script type='text/javascript'>
  $(document).ready(function() {
    if (config.fixednavbar) {
      var navbar = $(&#39;.navbar&#39;), placeholder = navbar.parent();
      $(window).on(&#39;resize load&#39;, function(){
        navbar.css({position: &#39;fixed&#39;, width: placeholder.outerWidth()+&#39;px&#39;, zIndex: &#39;9999999&#39;});
        placeholder.css({height: navbar.outerHeight()+&#39;px&#39;});
      });
    }
  });
</script>

<script type='text/javascript'>
  (function(){
    function init(){
      var anchor = document.querySelector('#loadingbar');
      <!-- Only attach near output element; avoid showing on non-safelink pages -->
      var outSel = (window.config &amp;&amp; window.config.output) ? window.config.output : null;
      var outEl = outSel ? document.querySelector(outSel) : null;
      if(!anchor &amp;&amp; outEl &amp;&amp; outEl.parentNode){
        var mount = document.createElement('div');
        outEl.parentNode.insertBefore(mount, outEl);
        anchor = mount;
      }
      <!-- If neither explicit loadingbar nor output exists, abort -->
      if(!anchor) return;

      var container = document.createElement('div');
      container.className = 'loading-container';
      var track = document.createElement('div');
      track.className = 'progress-track';
      var fill = document.createElement('div');
      fill.className = 'progress-fill';
      track.appendChild(fill);
      var text = document.createElement('div');
      text.className = 'progress-text';
      text.textContent = '5s';
      track.appendChild(text);
      container.appendChild(track);
      // Below-bar waiting note (XML-safe)
      var note = document.createElement('div');
      note.className = 'loading-note';
      note.textContent = 'Waiting...';
      container.appendChild(note);
      anchor.appendChild(container);

      var btn = document.createElement('button');
      btn.type = 'button';
      btn.className = 'scroll-btn';
      // Avoid inline HTML strings in XML; build icon + text via DOM
      var icon = document.createElement('i');
      icon.className = 'fas fa-arrow-down';
      btn.appendChild(icon);
      btn.appendChild(document.createTextNode(' ' + 'Scroll Down'));
      btn.style.display = 'none';
      anchor.appendChild(btn);

      var duration = 5000;
      var start = Date.now();
      var timer = setInterval(function(){
        var elapsed = Date.now() - start;
        var pct = Math.min(elapsed / duration * 100, 100);
        fill.style.width = pct + '%';
        var remaining = Math.ceil((duration - elapsed)/1000);
        text.textContent = (remaining &gt; 0 ? remaining : 0) + 's';
        if (elapsed &gt;= duration) {
          clearInterval(timer);
          if (container &amp;&amp; container.parentNode) container.parentNode.removeChild(container);
          btn.style.display = 'inline-flex';
        }
      }, 80);

      btn.addEventListener('click', function(){
        // Hide/disable after first click
        btn.disabled = true;
        btn.style.display = 'none';
        // Show the output action button only now
        try {
          if (typeof window.__showOutputActionButton === 'function') {
            window.__showOutputActionButton();
          }
        } catch(e) {}
        var ft = document.querySelector('footer.footer');
        if (ft &amp;&amp; typeof ft.scrollIntoView === 'function') {
          ft.scrollIntoView({ behavior: 'smooth', block: 'end' });
        } else {
          window.scrollTo({ top: (document.documentElement || document.body).scrollHeight, behavior: 'smooth' });
        }
      });
    }
    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', init);
    } else {
      init();
    }
  })();
</script>

<b:defaultmarkups><b:defaultmarkup type='Common'><b:includable id='body'><b:if cond='data:view.isPreview'><body/><b:else/>&lt;!--<body/>--&gt;</b:if></b:includable></b:defaultmarkup></b:defaultmarkups>&lt;/body&gt;
</html>